{
  "name": "vBackdrop",
  "description": "",
  "props": [
    {
      "type": [
        "String"
      ],
      "default": "\"backdrop\"",
      "globalDefault": true,
      "prop": "base",
      "description": "Base name used to style component"
    },
    {
      "type": [
        "String"
      ],
      "default": "\"screen\"",
      "prop": "fill",
      "description": "Position of backdrop. Can be either `screen` or `element`. Those are equivalent to respectively `fixed` or `absolute` class added to backdrop component."
    },
    {
      "type": [
        "Boolean"
      ],
      "default": true,
      "prop": "is-open",
      "description": "This prop controls visibility state of backdrop. It is `true` by default so if this prop is not defined backdrop will always be visible."
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "globalDefault": true,
      "prop": "mod-backdrop",
      "description": "Style of main backdrop element"
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "prop": "name",
      "description": "Name of the component"
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "prop": "variant",
      "description": "Active variant of the element `mod-*` props"
    }
  ],
  "events": [],
  "slots": [
    {
      "slot": "default",
      "description": "Slot to place content inside backdrop."
    }
  ],
  "functions": [],
  "components": []
}