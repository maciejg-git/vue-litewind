{
  "name": "vPagination",
  "description": "",
  "props": [
    {
      "type": [
        "Number"
      ],
      "default": "undefined",
      "prop": "v-model",
      "description": "Current page"
    },
    {
      "type": [
        "String"
      ],
      "default": "\"pagination\"",
      "globalDefault": true,
      "prop": "base",
      "description": "Base name used to style component"
    },
    {
      "type": [
        "Number"
      ],
      "default": "undefined",
      "prop": "items-count",
      "description": "Total number of items. Pagination uses this value to compute the number of pages to render. It is usually provided by another component that uses pagination for navigation"
    },
    {
      "type": [
        "Number"
      ],
      "default": 0,
      "prop": "items-per-page",
      "description": "Number of items per page. Pagination uses this value to compute the number of pages to render. It is usually provided by another component that uses pagination for navigation. `0` disables pagination (displays only 1 page)"
    },
    {
      "type": [
        "Number"
      ],
      "default": "undefined",
      "prop": "max-pages",
      "description": "Maximum number of pages to display. This prop must have value of 3 or more"
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "globalDefault": true,
      "prop": "mod-button",
      "description": ""
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "globalDefault": true,
      "prop": "mod-dots",
      "description": "Style of the dots separating pages"
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "globalDefault": true,
      "prop": "mod-page",
      "description": "Style of the single page button"
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "globalDefault": true,
      "prop": "mod-pagination-bar",
      "description": "Main pagination bar element"
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "prop": "name",
      "description": "Name of the component"
    },
    {
      "type": [
        "String"
      ],
      "default": "empty string",
      "prop": "variant",
      "description": "Active variant of the element `mod-*` props"
    }
  ],
  "events": [
    {
      "event": "update:modelValue",
      "description": "Updates v-model of component"
    }
  ],
  "slots": [],
  "functions": [],
  "components": []
}