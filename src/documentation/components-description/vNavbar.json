{
  "name": "vNavbar",
  "props": {
    "base": "",
    "variant": "",
    "fixed": "If `true` navbar position is set to fixed. If both fixed and sticky are `false` then navbar is positioned as relative",
    "sticky": "If `true` navbar position is set to sticky. If both fixed and sticky are `false` then navbar is positioned as relative",
    "bottom": "Places navbar on the bottom. Note: `fixed` must be also set to true",
    "modNavbar": "Main navbar element"
  },
  "emits": {},
  "slots": {
    "default": "Slot for navbar content"
  },
  "functions": {},
  "components": {}
}
